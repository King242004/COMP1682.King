// Profile option keys. Display labels live in the i18n catalog (t.labels.*)
// so Profile, Edit and Community all read one translated source.
export const GOAL_KEYS = ["lose_weight", "gain_muscle", "eat_healthy"] as const;
export const ACTIVITY_KEYS = ["sedentary", "moderate", "active"] as const;
export const CONDITION_KEYS = ["diabetes", "hypertension", "gout", "high_cholesterol", "gastritis", "none"] as const;
