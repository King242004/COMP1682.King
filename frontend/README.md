# MealMate mobile app

Ứng dụng React Native chạy bằng Expo Router.

```bash
npm install
npx expo start
```

Backend local mặc định chạy tại cổng `5000`. Có thể đặt `EXPO_PUBLIC_API_URL` trong `.env` để dùng backend khác.
